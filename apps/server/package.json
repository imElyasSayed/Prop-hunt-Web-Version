{
  "name": "splotch-server",
  "version": "0.1.0",
  "private": true,
  "description": "SPLOTCH authoritative multiplayer server (Colyseus).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@colyseus/core": "^0.16.0",
    "@colyseus/schema": "^3.0.0",
    "@colyseus/ws-transport": "^0.16.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "colyseus.js": "^0.16.0",
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
